# 세션 규칙 — 매 세션 시작·compact 뒤에도 **항상** 적용

CLAUDE.md 가 이 파일을 불러온다(`@SESSION.md`). 세션을 새로 열거나 대화가 요약돼도 여기는 그대로 다시 읽힌다.
**개발 작업 방식** 중 반복해서 어긴 것만 모았다 (사이트 내용 규칙은 PROJECT-GUIDE·스킬 몫) — 규칙이 CLAUDE.md·스킬·기억과 부딪히면 이 파일이 이긴다 (사용자가 직접 정한 것들이다).

## 0. 세션 시작하면

1. `git status` · `git log --oneline origin/main..HEAD` — 배포를 기다리는 **로컬 커밋·미커밋 수정**이 있는지 본다.
2. dev 서버가 떠 있는지 본다 (브라우저 패널 `preview_list`). 없으면 `preview_start` 로 `terra-archive` 를 띄운다.

## 1. 빌드·커밋·배포 — 가장 많이 어긴 것

- **`npm run build` 는 배포할 때만 돌린다.** 커밋마다도, 작업 한 덩어리가 끝날 때마다도 아니다.
  작업 중 확인은 dev(핫리로드) + 브라우저 실측 + 개별 가드(`npx tsc --noEmit -p .` · `node scripts/check-css.mjs`)로 한다.
  (사용자: "빌드는 배포할 때만 하기로 했잖아 왜 계속 빌드를 해" — 2026-09-23, 두 번째 지적)
- **커밋·푸시·빌드·배포는 배포 직전에 한꺼번에.** 요청 하나마다 커밋·푸시하지 않는다.
  수정이 끝나면 "dev 에서 확인했다 · 배포 때 커밋·빌드·푸시한다"고만 보고한다.
- **배포(`bash scripts/deploy.sh`)는 사용자가 하라고 할 때만.** deploy.sh 가 빌드·R2 동기화까지 한다.
- 커밋할 때:
  - 내가 고친 파일만 **경로로 집어** 스테이징한다 — `git add -A` 금지 (다른 세션 작업이 트리에 있을 수 있다).
  - `public/sitemap.xml` 은 빌드 부산물이다 → `git checkout -- public/sitemap.xml`.
  - CI 가 매일 "데이터 자동 갱신" 커밋을 올린다 → 푸시 전에 `git fetch` 후 rebase.

## 2. 로컬 서버

- `npm run dev` 를 **상시 켜 두고 끄지 않는다.** 빌드한다고 내릴 필요도 없다.
- `npm run start`(vinext start) 금지 — `/admin` 이 dev 전용이고, 같은 3000 포트를 물어 dev 와 부딪힌다.

## 3. 확인·보고

- **"됐다"는 사용자 화면 기준이다.** 구현만 바꾸고 겉모습이 그대로면 안 된 것이다. 브라우저로 실측한 뒤에만 말한다.
  "안 됐다"는 말에 배포·캐시 탓을 하지 말고, 그 URL 부터 열어 본다.
- **요청 밖 픽셀 금지.** 위치·크기·정렬은 요청받은 것만 바꾼다. 해석이 갈리면 지금 상태를 보여 주고 묻는다.
- **겹치기·정렬은 위치가 하나뿐인 표식으로 확인한다.** 균일한 격자선처럼 반복되는 특징은 배율·뒤집기가
  틀려도 맞아 보인다 — 출현·도착 칸 같은 표식이 제자리에 오는지 대조한다 (2026-09-23 생존연산 경로 어긋남).
- **스크린샷은 세션 한도(~15장)가 있다.** 수치 검증(JS로 좌표·색·개수)을 먼저 하고, 찍을 땐 한 장에 여러 상태를 담는다.
  브라우저 패널의 `zoom` 은 동작하지 않는다(전체 화면이 다시 온다) — 쓰지 말 것.
- 보고는 한국어로, 짧게.

## 4. 제안(피드백) 처리

- 읽기 → 검증 → 보고에서 **멈춘다.** 고치는 건 사용자 승인 뒤에만.
- 제보자 답변·대응완료 표시는 **또 따로** 승인받는다 (밖으로 나가는 행동).
